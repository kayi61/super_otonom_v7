# super_otonom — örnek ortam (değerleri kopyalayıp .env yapın; .env commitlenmez)
# Ayrıntı: docs/RUNBOOK.md

# ---- Güven (önce simülasyon) ----
DRY_RUN=true
PAPER_MODE=true
LIVE_CONFIRM=
# Canlı: PAPER_MODE=false ve LIVE_CONFIRM=YES birlikte

# ---- Ağ / döngü ----
DEFAULT_EXCHANGE=binance
POLL_INTERVAL_SEC=30
LOG_LEVEL=INFO
INITIAL_CAPITAL=1000

# ---- OMEGA / kalite ----
SIGNAL_QUALITY_MIN=50
ML_SERVICE_ENABLED=false
ML_SERVICE_URL=
ML_SERVICE_TIMEOUT=2.0

# ---- Risk (örnek) ----
MAX_OPEN_POSITIONS=1
# MAX_POSITION_COUNT=3
STOP_LOSS_PCT=0.02
MAX_POSITION_PCT=0.05
TAKE_PROFIT_PCT=0.03

# ---- Borsa ----
# Kamu OHLCV için ana ağ: testnet bağlantı sorunu yaşanırsa false deneyin.
BINANCE_TESTNET=false
# BINANCE_API_KEY=
# BINANCE_API_SECRET=

# ---- Metrik (isteğe bağlı) ----
# METRICS_PORT=8000



# ---- Borsa ----
BINANCE_TESTNET=false

# Şu anlık bunlara dokunma, başında # kalsın:
# BINANCE_API_KEY=senin_keyin_buraya
# BINANCE_API_SECRET=senin_secretin_buraya

# ---- Güvenlik & Simülasyon ----
DRY_RUN=true
PAPER_MODE=true
SIGNAL_QUALITY_MIN=50
