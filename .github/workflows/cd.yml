name: CD (Docker Build + GHCR Push + Staging Deploy)

# main merge / v* tag → GHCR image + staging smoke deploy
on:
  push:
    branches: [main]
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      image: ${{ steps.meta.outputs.image }}
      repo: ${{ steps.meta.outputs.repo }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Resolve semantic version from git tag
        id: version
        shell: bash
        run: |
          set -euo pipefail
          git fetch --tags --force
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            version="${GITHUB_REF_NAME#v}"
          else
            tag="$(git describe --tags --abbrev=0 --match 'v[0-9]*' 2>/dev/null || true)"
            version="${tag#v}"
          fi
          if [[ -z "${version}" ]]; then
            version="0.0.0"
          fi
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Docker image name (GHCR lowercase)
        id: meta
        shell: bash
        run: |
          repo_lc="$(echo "${GITHUB_REPOSITORY}" | tr '[:upper:]' '[:lower:]')"
          echo "image=ghcr.io/${repo_lc}" >> "$GITHUB_OUTPUT"
          echo "repo=${repo_lc}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image (GHCR)
        env:
          VERSION: ${{ steps.version.outputs.version }}
          IMAGE: ${{ steps.meta.outputs.image }}
        shell: bash
        run: |
          set -euo pipefail
          build_date="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          short_sha="${GITHUB_SHA:0:12}"
          docker buildx build \
            --push \
            --tag "${IMAGE}:${VERSION}" \
            --tag "${IMAGE}:${short_sha}" \
            --tag "${IMAGE}:latest" \
            --build-arg BUILD_VERSION="${VERSION}" \
            --build-arg VCS_REF="${GITHUB_SHA}" \
            --build-arg BUILD_DATE="${build_date}" \
            --build-arg BUILD_SOURCE="https://github.com/${GITHUB_REPOSITORY}" \
            .

  deploy_staging:
    needs: build_and_push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Create CI .env (compose env_file)
        shell: bash
        run: |
          set -euo pipefail
          cp .env.template .env
          sed -i 's/^POSTGRES_PASSWORD=.*/POSTGRES_PASSWORD=ci-compose-validate/' .env
          sed -i 's/^GRAFANA_PASSWORD=.*/GRAFANA_PASSWORD=ci-compose-validate/' .env

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Staging deploy (redis + vault + bot)
        env:
          BOT_IMAGE_REPO: ${{ needs.build_and_push.outputs.repo }}
          BOT_VERSION: ${{ needs.build_and_push.outputs.version }}
          IMAGE: ${{ needs.build_and_push.outputs.image }}
        shell: bash
        run: |
          set -euo pipefail
          export BOT_IMAGE_REPO="${BOT_IMAGE_REPO}"
          export BOT_VERSION="${BOT_VERSION}"
          docker compose -f docker-compose.yml -f docker-compose.staging.yml config --quiet
          docker pull "${IMAGE}:${BOT_VERSION}"
          docker compose -f docker-compose.yml -f docker-compose.staging.yml pull redis vault vault-init || true
          docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d redis
          docker compose -f docker-compose.yml -f docker-compose.staging.yml up vault-init
          docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d vault bot --no-build
          docker compose -f docker-compose.yml -f docker-compose.staging.yml ps
